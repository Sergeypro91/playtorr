export enum MediaType {
	MOVIE = 'movie',
	TV = 'tv',
	PERSON = 'person',
}

export enum TimeWindow {
	DAY = 'day',
	WEEK = 'week',
}

export enum EnumStatus {
	CREATED = 'created',
	UPDATING = 'updating',
	FINISHED = 'finished',
	WARNING = 'warning',
	ERROR = 'error',
}
